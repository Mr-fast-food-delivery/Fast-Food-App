# ===============================
# APPLICATION
# ===============================
spring.application.name=FoodApp
server.port=8091

# ===============================
# MYSQL TEST (GITHUB ACTIONS)
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/foodapp_test
spring.datasource.username=test
spring.datasource.password=test
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# JPA / HIBERNATE (TEST)
# ===============================
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false

# ===============================
# ACTUATOR (CI HEALTH CHECK)
# ===============================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# ===============================
# JWT (TEST ONLY)
# ===============================
secreteJwtString=test_jwt_secret_ci_only

# ===============================
# MAIL (DISABLED)
# ===============================
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=test
spring.mail.password=test
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# ===============================
# AWS (DUMMY)
# ===============================
aws.s3.region=us-east-2
aws.s3.bucket=test-bucket
aws.accessKeyId=dummy
aws.secretKey=dummy

# ===============================
# STRIPE (DUMMY)
# ===============================
stripe.api.public.key=test_public_key
stripe.api.secret.key=test_secret_key

# ===============================
# FRONTEND (LOCAL)
# ===============================
base.payment.link=http://localhost:3000/pay?orderid=
frontend.base.url=http://localhost:3000
